#!/bin/bash
# One Electron runtime for Maya and the Zoth desktop apps.
export LD_LIBRARY_PATH="/opt/electron:${LD_LIBRARY_PATH:-}"
if [[ -x /opt/electron/electron ]]; then
  exec /opt/electron/electron --no-sandbox "$@"
fi
echo "Electron is not installed under /opt/electron." >&2
exit 1
